version: '3.8'

services:
  agent-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - API_PORT=8000
      - API_HOST=0.0.0.0
    volumes:
      - .:/app
    command: python app.py
    restart: unless-stopped
